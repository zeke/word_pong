page << "$('#ping_display').html('#{@related_words.first}');"
page << "$('#ping').val('#{@related_words.first}');"
page << "$('#pong').val('');"
page << "$('#blacklist').val('#{@blacklist}');"
page << "$('#ping_pong_precedent_id').val('#{@ping_pong.id}');"